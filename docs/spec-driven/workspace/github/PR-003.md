# PR #3 — Drag and Drop

**Branch:** `feat/drag-drop` → `main`
**Author:** Developer
**Reviewers:** @tech-lead
**Labels:** `feature` `interaction`
**Linked ticket:** ROT-003
**Status:** ✅ Merged — after one Bugbot fix

---

## Summary

- Drag to reorder projects within sections
- Drag to move projects between sections
- Timeline entry recorded on section move
- Keyboard-accessible drag handles (dnd-kit built-in)

## What changed

| File | Change |
|---|---|
| `src/components/garden/GardenBoard.tsx` | Wrapped in `DndContext` |
| `src/components/garden/Section.tsx` | Added `SortableContext`, `useDroppable` |
| `src/components/garden/ProjectCard.tsx` | Added `useSortable`, mounting guard |
| `src/context/GardenContext.tsx` | Added `REORDER_PROJECTS`, `MOVE_PROJECT` actions |

## How to test

1. Drag a card within a section — verify reorder persists on reload
2. Drag a card to another section — verify move persists on reload
3. Drag to invalid area — card returns to original position
4. Check project timeline — move should add a `TimelineEntry`

## Confidence Map summary

| Decision | |
|---|---|
| dnd-kit mounting guard on ProjectCard | 🟢 GREEN — ADR-004 + ADR-006 |
| Timeline entry on MOVE_PROJECT | 🟢 GREEN — AC #5 explicit |

---

## Automated review results

**Spec compliance check:** ✅ All 5 ACs GREEN

**Bugbot:** ❌ 1 violation found (resolved before merge)

> **Bugbot finding:** `ProjectCard.tsx` line 47 — `useSortable` component detected. No client-side mounting guard found in this file. This causes a hydration mismatch in production (ADR-004 + ADR-006). Required pattern:
> ```tsx
> const [mounted, setMounted] = useState(false)
> useEffect(() => setMounted(true), [])
> if (!mounted) return <StaticCard {...props} />
> ```

**Developer fix:** Added mounting guard. Re-run: ✅

**Tech lead review:** ✅

---

## Review comments

**@tech-lead:**
> Good catch by Bugbot — this is exactly the hydration pattern we've seen bite us before. The mounting guard fix is correct.  
> The `MOVE_PROJECT` reducer correctly adds the `TimelineEntry` — verified. LGTM.

**Merged by:** @tech-lead
