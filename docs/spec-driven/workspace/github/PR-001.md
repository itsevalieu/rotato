# PR #1 — Garden Board

**Branch:** `feat/garden-board` → `main`
**Author:** Developer
**Reviewers:** @tech-lead
**Labels:** `feature` `board`
**Linked ticket:** ROT-001
**Status:** ✅ Merged

---

## Summary

- Implements the core four-section board layout (Currently Playing, Resting, Seeds, Finished Worlds)
- Collapsible sections with persistence across page reloads
- Project card component with color bar, icon, tags, hover menu
- Hydration guard on project count display (ADR-006)

## What changed

| File | Change |
|---|---|
| `src/components/garden/GardenBoard.tsx` | New — root board component |
| `src/components/garden/Section.tsx` | New — collapsible section with dnd-kit drop zone |
| `src/components/garden/ProjectCard.tsx` | New — project card with hover menu |
| `src/context/GardenContext.tsx` | Added `TOGGLE_SECTION_COLLAPSE` action |
| `src/lib/types.ts` | Added `GardenState`, `Project`, `SectionId` types |

## How to test

1. Load app — verify 4 sections appear with correct labels and empty states
2. Add a project — verify it appears in the correct section
3. Collapse a section — verify collapse persists on reload
4. Hover a project card — verify Edit / Move / Archive menu appears

## Confidence Map summary

| Decision | |
|---|---|
| Section order fixed (not user-configurable) | 🟢 GREEN — SECTION_ORDER constant, matches PRD |
| `collapsedSections` in GardenState | 🟢 GREEN — ADR-002 |
| Hydration guard on project count | 🟢 GREEN — ADR-006 |

---

## Automated review results

**Spec compliance check:** ✅ All 5 ACs GREEN

**Bugbot:** ✅ No violations

**Tech lead review:** ✅ ADRs respected

**Design review:** ✅ Colors from palette, Caveat for section headers, Inter for body

---

## Review comments

**@tech-lead:**
> Section empty states look good. One thing — `SECTION_META.emptyMessage` should come from the constants, not be hardcoded. I see you already did that. 
> LGTM.

**Merged by:** @tech-lead
